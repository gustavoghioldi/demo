version: 2
jobs:
  #maven-jobs:
  build: 
    machine:
      java:
        version: oraclejdk8
    steps:
      - checkout
      - run:
          name: test
          command: mvn test
      - run: pwd
      - run:
          name: package
          command: mvn package
      